QA staging for pricewatch. Contains `TEST_PLAN.md` (acceptance criteria, edge cases, manual smoke checklist), eight `fixtures/` files representing realistic raw output from PDF extraction or OCR (two bank statements: Chase US and HDFC India; five receipts: Walmart, Costco, Indian kirana, Amazon order, plus a deliberately OCR-corrupted Walmart variant), and `fixtures/expected_parsed_walmart.json` as the golden file for parser unit tests against `receipt_walmart.txt`. Copy these into the real repo under `tests/fixtures/` and `docs/` (or wherever the dev keeps them); the fixtures preserve messy spacing on purpose, do not reformat them. Run the parser against each fixture and diff against the expected JSON for walmart; the others are stress tests for the generic fallback and merchant-rule learner.
